programa {
  funcao inicio() {
    real a, b, c, d, e, f, x, y
    leia(a)
    leia(b)
    leia(c)
    leia(d)
    leia(e)
    leia(f)
    x = ((c * e) - (b * f)) / ((a * e) - (b * d))
    y = ((a * f) - (c * d)) / ((a * e) - (b * d))
    escreva("x = ", x, " y = ", y)
  }
}
